# Postgres runtime (Supabase: dùng pooled URL port 6543 với ?pgbouncer=true)
DATABASE_URL=""
# Kết nối direct cho `prisma migrate deploy` (Supabase: port 5432). Local: trùng DATABASE_URL.
DIRECT_URL=""
AUTH_SECRET=""
# Để trống khi deploy Vercel (đã trustHost). Local: http://localhost:3000
AUTH_URL=""
